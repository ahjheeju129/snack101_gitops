# .github/workflows/cd.yaml 생성
name: CD
on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy to ArgoCD
        run: |
          # ArgoCD 연동 로직