apiVersion: apps/v1
kind: Deployment
metadata:
  name: snack101-app
  namespace: snack101
  labels:
    app: snack101
spec:
  replicas: 2
  selector:
    matchLabels:
      app: snack101
  template:
    metadata:
      labels:
        app: snack101
    spec:
      imagePullSecrets:
      - name: ecr-registry-secret
      containers:
      - name: snack101
        image: 036670977407.dkr.ecr.ap-northeast-2.amazonaws.com/snack101:260072759e22ab0a1babf9e5a4ffa56eeea2e13b
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          valueFrom:
            configMapKeyRef:
              name: snack101-config
              key: DATABASE_URL
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
